jdk:
  - openjdk21

before_install:
  - chmod +x gradlew

install:
  # 强制指向 Linux 标准 JDK 21 路径，不依赖 sdkman 的动态安装
  - export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 && ./gradlew publishToMavenLocal -x :theme:lintAnalyzeDebug -x test --no-daemon
