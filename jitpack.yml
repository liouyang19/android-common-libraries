jdk:
  - openjdk21

before_install:
  - chmod +x gradlew
  - unset JAVA_HOME
  - java -version
  - ./gradlew -v

install:
  - unset JAVA_HOME && ./gradlew build :bom:publishToMavenLocal :theme:publishToMavenLocal -x :theme:lintAnalyzeDebug -x test -x androidTest --no-daemon --stacktrace --info
