jdk:
  - openjdk21

before_install:
  - unset JAVA_HOME
  - export PATH=/usr/lib/jvm/java-21-openjdk-amd64/bin:$PATH
  - java -version
  - chmod +x gradlew

install:
  - ./gradlew publishToMavenLocal -x :theme:lintAnalyzeDebug -x test --no-daemon --stacktrace